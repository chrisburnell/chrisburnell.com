---
layout: feed
searchable: false
sitemap:
  exclude: true

title: Bookshelf Feed
lede: All of my bookshelf check-ins.
category_path: '/bookshelf/'
---

{%- assign limit = site.feed_limit -%}
{% assign books_to_read = site.books | where_exp: 'book', 'book.date_start == nil' | sort: 'title' %}
{% for page in books_to_read %}
    {%- assign limit = limit | minus: 1 -%}
    {% include components/item-feed.html %}
{% endfor %}
{% assign books = site.books | where_exp: 'book', 'book.date_start' | sort: 'date_start' | reverse %}
{% for page in books limit: limit %}
    {% include components/item-feed.html %}
{% endfor %}
