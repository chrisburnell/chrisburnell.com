name: Build
description: Build the site

runs:
  using: composite
  steps:
    - name: Set up site cache
      id: cache-site
      uses: actions/cache@v4
      with:
        key: site-cache
        path: |
          .cache
          _site

    - name: Build with npm
      run: npm run build-production
      shell: bash
