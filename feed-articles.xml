---
layout: null
description: All of my articles.
---

<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0">

    <channel>

        <title>{{ site.title | xml_escape }} – Articles Only</title>
        <description>{{ page.description | xml_escape }}</description>
        <link>{{ site.url }}{{ site.baseurl }}/articles/</link>
        <language>en</language>

        <image>
            <url>{{ site.url }}{{ site.baseurl }}/favicon.png</url>
            <title>{{ site.author }}</title>
            <link>{{ site.url }}{{ site.baseurl }}/</link>
        </image>

        {% for post in site.posts %}
            <item>
                <title>{{ post.title }}</title>
                <link>{{ post.url | prepend: site.baseurl | prepend: site.url }}</link>
                <pubDate>{{ post.date | date_to_xmlschema }}</pubDate>
                <category>{{ post.category }}</category>
                {% if post.introduction %}<description>{{ post.introduction }}</description>{% endif %}
            </item>
        {% endfor %}

    </channel>

</rss>
