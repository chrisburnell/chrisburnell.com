name: Check
description: Check build for errors

runs:
  using: composite
  steps:
    - name: Check Build
      run: npm run check:build
      shell: bash

    - name: Check DateTimes
      run: npm run check:datetimes
      shell: bash
