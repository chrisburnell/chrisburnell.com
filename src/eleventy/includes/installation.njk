{% if npm -%}
Install the package, which is <a href="https://www.npmjs.com/package/{{ npm }}" rel="external noopener">available on npm</a>:

```bash
npm install {{ npm }}{% if dev %} --save-dev{% endif %}
```
{%- endif %}

{% if git -%}
You can also download it directly <a href="https://github.com/{{ git }}" rel="external noopener">from GitHub</a>:

<samp><a href="https://github.com/{{ git }}/archive/{{ branch | default('main')}}.zip" rel="external noopener">https://github.com/{{ git }}/archive/{{ branch | default('main')}}.zip</a></samp>
{%- endif %}
