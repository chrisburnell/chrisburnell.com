---
layout: base
title: All Posts
description: Browse all of my posts by a variety of metrics.
hfeed: true
show_photos: true
truncate: true
---
{%- set collection = collections.posts -%}
{%- set count = collection.length -%}
{%- set latest_year = collection[0].date.getFullYear() -%}
{%- set first_year = collection[count-1].date.getFullYear() -%}
<div id="content" class=" [ pantheon ] [ wrap ] [ archive ] ">

	<h1 id="title" class=" [ title ] ">
		<span class=" [ p-name ] ">{{ title | safe }}</span>
	</h1>

	<div class=" [ description ] [ flow ] [ p-summary ] ">
		<p>Browse all {{ collections.posts.length }} of my posts by a variety of metrics, dating from {{ first_year }}–{{ latest_year }}.</p>
	</div>

	{% sparkline collection, first_year, latest_year %}

	<div class=" [ content ] [ flow ] [ h-feed ] ">
		{% for lookup in collections.categories %}
			{% if not loop.first %}
				<hr>
			{% endif %}
			{% set lookup_collection = collections[lookup.title] | arePublished | reverse %}
			<h2 id="{{ lookup.plural }}">
				<a href="/{{ lookup.plural }}/">{{ lookup.properPlural | title }}</a>
			</h2>
			{% if lookup_collection[0].data.list == 'shelf' %}
				<nav class=" [ grid ] [ shelf {% if lookup_collection[0].data.read_of %}shelf--books {% endif %}] " aria-labelledby="{{ lookup.plural }}">
					{%- for item in lookup_collection | limit(site.limits.shelf) -%}
						{% include 'items/shelf.njk' %}
					{%- endfor -%}
				</nav>
				{% if lookup_collection.length > site.limits.shelf %}
					<nav class=" [ grid ] [ navigator ] " aria-label="All {{ lookup.properPlural | title }}">
						<a class=" [ button ] " href="/{{ lookup.plural }}/">All {{ lookup.properPlural | title }}</a>
					</nav>
				{% endif %}
			{% else %}
				<nav class=" [ deck ] " aria-labelledby="{{ lookup.plural }}">
					{%- for item in lookup_collection | limit(site.limits.deck) -%}
						{% include 'items/deck.njk' %}
					{%- endfor -%}
				</nav>
				{% if lookup_collection.length > site.limits.deck %}
					<nav class=" [ grid ] [ navigator ] " aria-label="All {{ lookup.properPlural | title }}">
						<a class=" [ button ] " href="/{{ lookup.plural }}/">All {{ lookup.properPlural | title }}</a>
					</nav>
				{% endif %}
			{% endif %}
		{% endfor %}

		{% include 'responses.njk' %}
	</div>

	<div class=" [ meta ] [ flow ] ">
		{% include 'meta/archive.njk' %}
	</div>

	{% include 'ads.njk' %}

</div>
