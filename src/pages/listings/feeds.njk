---
layout: base
title: Feeds
tagline: '<c-emoji><svg width="60" height="60" aria-hidden="true" focusable="false" style="fill: var(--color-rss); margin-inline-end: 0.5ch;"><use href="#svg--rss"></use></svg></c-emoji>Feeds'
description: You can follow my posts from different web feeds, depending on what content you'd like to see.
perch: true
---
{% set collection = collections.posts -%}
{% set count = collection.length -%}
{% set latest_year = collection[0].date.getFullYear() -%}
{% set first_year = collection[count-1].date.getFullYear() -%}
<div class=" [ pantheon ] [ wrap ] [ archive ] [ perch ] ">

	<h1 id="title" class=" [ title ] ">
		<span class=" [ p-name ] ">{{ tagline | default(title) | safe }}</span>
	</h1>

	<div class=" [ description ] [ flow ] [ p-summary ] ">
		{{ descriptionLong | default(description) | formatAsMarkdown | safe }}
	</div>

	{% sparkline collection, first_year, latest_year %}

	<div id="content" class=" [ content ] [ flow ] ">
		{% include 'css-naked-day.njk' %}

		<h2>
			<c-emoji><svg width="32" height="32" aria-hidden="true" focusable="false" style="fill: var(--color-rss); vertical-align: middle; margin-block-start: calc(1ex - 1cap);"><use href="#svg--rss"></use></svg></c-emoji> Collections
		</h2>

		<ul class=" [ grid ] [ shelf ] ">
			<li>
				<a href="{{ site.url + '/feed.xml' }}" class=" [ strong ] " style="font-size: var(--font-size-gamma);">Latest Posts</a>
				<p>Latest posts on the blog.</p>
			</li>
			<li>
				<a href="{{ site.url + '/writing.xml' }}" class=" [ strong ] " style="font-size: var(--font-size-gamma);">Writing</a>
				<p>A more serious collection of ideas and bits of prose.</p>
			</li>
			<li>
				<a href="{{ site.url + '/replies.xml' }}" class=" [ strong ] " style="font-size: var(--font-size-gamma);">Replies</a>
				<p>Replies I’ve made to other people.</p>
			</li>
			<li>
				<a href="{{ site.url + '/rsvps.xml' }}" class=" [ strong ] " style="font-size: var(--font-size-gamma);">RSVPs</a>
				<p>A record of RSVPs to conferences and meetups.</p>
			</li>
		</ul>

		<h2 id="categories">
			<c-emoji><svg width="32" height="32" aria-hidden="true" focusable="false" style="fill: var(--color-rss); vertical-align: middle; margin-block-start: calc(1ex - 1cap);"><use href="#svg--rss"></use></svg></c-emoji> Categories
		</h2>

		<ul class=" [ grid ] [ shelf ] " data-layout="natural">
		{% for item in collections.categories -%}
			{%- set count = collections[item.title] | arePublished | getCollectionCount -%}
			<li><a href="{{ site.url }}/{{ item.plural }}.xml" class=" [ strong ] " style="font-size: var(--font-size-gamma);"><c-emoji>{{ item.emoji | default('📄') }}</c-emoji>{{ item.properPlural | title }} <sup>{{ count }}</sup></a></li>
		{%- endfor %}
		</ul>

		<h2 id="tags">
			<c-emoji><svg width="32" height="32" aria-hidden="true" focusable="false" style="fill: var(--color-rss); vertical-align: middle; margin-block-start: calc(1ex - 1cap);"><use href="#svg--rss"></use></svg></c-emoji> Tags
		</h2>

		<ul class=" [ grid ] [ shelf ] " data-layout="natural">
		{%- set lowest = 9001 -%}
		{%- set highest = 0 -%}
		{%- for item in collections.tags | sort -%}
			{%- set count = collections[item] | arePublished | getCollectionCount -%}
			{%- if count < lowest -%}
				{%- set lowest = count -%}
			{%- elif count > highest -%}
				{%- set highest = count -%}
			{%- endif -%}
		{%- endfor -%}
		{% for item in collections.tags | sort -%}
			{%- set count = collections[item] | arePublished | getCollectionCount -%}
			{%- set fontSize = count | rangeMap(lowest, highest, 1, 1.375, 3) -%}
			<li><a href="{{ site.url }}/{{ item }}.xml" class=" [ strong ] " style="font-size: {{ fontSize }}em;">{{ item }} <sup>{{ count }}</sup></a></li>
		{%- endfor %}
		</ul>

		<h2>Learn more</h2>

		<p>Visit <a href="https://aboutfeeds.com/">About Feeds</a> to get started with newsreaders and subscribing.</p>

		<div class=" [ grid ] [ navigator ] ">
			<a href="/posts/" class=" [ button ] ">Browse All Posts</a>
		</div>
	</div>

	<div class=" [ meta ] [ flow ] " data-pagefind-ignore>
		{% include 'meta/page.njk' %}
	</div>
</div>
