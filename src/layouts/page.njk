---
layout: base
---
<article id="content" class=" [ content ] [ wrap ] [ page ] ">

    <h1 id="title" class=" [ title{% if title_error %} title--negative{% elif title_success %} title--positive{% endif %} ] ">
        <span class=" [ p-name ] ">{{ tagline | default(title) | safe }}</span>
    </h1>

    <div class=" [ details ] ">
        {% if lede == site.lede %}
            <p class=" [ p-summary ] " aria-hidden="true">{{ lede | safe | replace('href=', 'tabindex="-1" href=') }}</p>
        {% elif lede %}
            <p class=" [ p-summary ] ">{{ lede | safe }}</p>
        {% endif %}
    </div>

    <div class=" [ content__body ] ">
        <div class=" [ e-content ] ">
            {{ content | safe }}
        </div>

        {% include 'webmentions.njk' %}
    </div>

    {% include 'ads.njk' %}

</article>
