<h3>Latest projects:</h3>
<div class=" [ meta__list ] ">
	{% for lookup in projects.personal | limit(site.limits.feature) %}
		<a href="{{ lookup.url }}">{{ lookup.data.title }}</a>
		{{ lookup.data.description | markdownFormat | safe }}
	{% endfor %}
</div>
<p class="skip-owl"><a href="/projects/" class="small">See all projects →</a></p>
